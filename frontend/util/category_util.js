export default {
  1: { name: "entertainment", id: 1},
  2: { name: "entertainment", id: 1},
  3:{ name: "entertainment", id: 1}
};

import categories from '../util/category_util';

// <select>
//   {Object.keys(categories).map(id => <option value={id}>{categories[id].name}<option>)}
// </select>
